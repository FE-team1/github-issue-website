import styled from "styled-components";

const NavBar = () => {
  return <S.Wrapper>NavBar</S.Wrapper>;
};

export default NavBar;

const Wrapper = styled.div`
  width: 300px;
  border: 1px solid black;
`;

const S = {
  Wrapper,
};
